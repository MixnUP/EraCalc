## TO BE MAINTAINED

- useEffects eat performance (do not nest)
- apply/ensure DRY to zustand stores
- always use stores directly (avoid prop drilling)
- do not use React.FC instead use modern react implementation
- ensure consistency (e.g. use export default function throughout all)